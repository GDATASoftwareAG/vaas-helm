apiVersion: v1
kind: Service
metadata:
  name: {{ include "vaas-helm.fullname" . }}
  labels:
    {{- include "vaas-helm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "vaas-helm.selectorLabels" . | nindent 4 }}
